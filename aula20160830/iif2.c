#include<stdio.h>
#include<math.h>

int main () {

    float n,fatorial = 1, i;
    printf("Digite um numero aleatorio: ");
    scanf("%ld",&n);
      printf("O resultado do fatorial e: %ld", n,fat(n));
    return 0;
}
